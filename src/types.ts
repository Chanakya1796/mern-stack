export interface Student {
  id: string;
  name: string;
  email: string;
  grade: string;
  major: string;
  enrollmentDate: string;
}

export interface Faculty {
  id: string;
  name: string;
  email: string;
  department: string;
  title: string;
  joinDate: string;
}